name: todo_getx
description: "A new Flutter Meal App Project using GetX State Management."

publish_to: 'www.qrdpro.com/app/nexus/meals'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  transparent_image: ^2.0.1
  google_fonts: ^6.2.1
  get: ^4.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
